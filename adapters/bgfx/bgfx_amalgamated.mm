/* Compiles the vendored bgfx amalgamated build as Objective-C++, which the
 * Metal renderer requires on Apple platforms. The vendored tree is not
 * modified; only the compilation mode changes. */
#include "amalgamated.cpp"

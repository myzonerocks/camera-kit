# NODE_JS/BINARYEN_ROOT/LLVM_ROOT come from the EM_NODE_JS/EM_BINARYEN_ROOT/
# EM_LLVM_ROOT environment variables build.zig sets per invocation; this file
# only needs to exist for em++'s config loader to read past it.

#!/bin/sh
# Source-tracked gate, both directions, plus the abi surface check on every
# commit.
set -e
root="$(git rev-parse --show-toplevel)"
zig="$root/.local/zig/current/zig"
[ -x "$zig" ] || zig=zig
cd "$root"
"$zig" build gate -- --staged
exec "$zig" build abi

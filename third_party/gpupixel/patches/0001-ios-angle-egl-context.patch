--- a/src/core/gpupixel_gl_include.h
+++ b/src/core/gpupixel_gl_include.h
@@ -10,8 +10,17 @@
 #include "gpupixel/gpupixel_define.h"
 #include "utils/logging.h"
 #if defined(GPUPIXEL_IOS)
-#import <OpenGLES/ES3/gl.h>
-#import <OpenGLES/ES3/glext.h>
+// Real OpenGL ES on iOS hardware is gone (EAGLContext creation fails
+// outright); this renders through ANGLE's EGL/GLES-over-Metal instead,
+// the same EGL entry points GPUPIXEL_ANDROID already uses below.
+#include <EGL/egl.h>
+#include <EGL/eglext.h>
+#include <EGL/eglext_angle.h>
+#include <GLES3/gl3.h>
+#include <GLES2/gl2ext.h>
+// Apple's native GLES headers expose this as plain GL_BGRA; ANGLE only
+// defines the standard GLES extension name for the same token (0x80E1).
+#define GL_BGRA GL_BGRA_EXT
 #import <UIKit/UIKit.h>
 #elif defined(GPUPIXEL_MAC)
 #import <AppKit/AppKit.h>
--- a/src/core/gpupixel_context.h
+++ b/src/core/gpupixel_context.h
@@ -33,7 +33,10 @@
   void PresentBufferForDisplay();
 
 #if defined(GPUPIXEL_IOS)
-  EAGLContext* GetEglContext() const { return egl_context_; };
+  EGLContext GetEglContext() const { return egl_context_; };
+  EGLDisplay GetEglDisplay() const { return egl_display_; };
+  EGLSurface GetEglSurface() const { return egl_surface_; };
+  EGLConfig GetEglConfig() const { return egl_config_; };
 #elif defined(GPUPIXEL_MAC)
   NSOpenGLContext* GetOpenGLContext() const {
     return image_processing_context_;
@@ -64,7 +67,10 @@
   std::shared_ptr<DispatchQueue> task_queue_;
 
 #if defined(GPUPIXEL_IOS)
-  EAGLContext* egl_context_;
+  EGLDisplay egl_display_;
+  EGLConfig egl_config_;
+  EGLSurface egl_surface_;
+  EGLContext egl_context_;
 #elif defined(GPUPIXEL_MAC)
   NSOpenGLPixelFormat* pixel_format_;
   NSOpenGLContext* image_processing_context_;
--- a/src/core/gpupixel_context.cc
+++ b/src/core/gpupixel_context.cc
@@ -77,14 +77,68 @@
 
 void GPUPixelContext::CreateContext() {
 #if defined(GPUPIXEL_IOS)
-  LOG_DEBUG("Creating iOS OpenGL ES 2.0 context");
-  egl_context_ = [[EAGLContext alloc] initWithAPI:kEAGLRenderingAPIOpenGLES2];
-  if (!egl_context_) {
-    LOG_ERROR("Failed to create iOS OpenGL ES 2.0 context");
+  // Real EAGLContext creation fails outright on current iOS hardware -
+  // OpenGL ES is gone there. This goes through ANGLE's EGL instead,
+  // forced onto its Metal backend, same shape as GPUPIXEL_ANDROID below.
+  LOG_DEBUG("Creating iOS EGL context (ANGLE/Metal)");
+  const EGLAttrib displayAttribs[] = {
+      EGL_PLATFORM_ANGLE_TYPE_ANGLE, EGL_PLATFORM_ANGLE_TYPE_METAL_ANGLE,
+      EGL_NONE};
+  egl_display_ = eglGetPlatformDisplay(EGL_PLATFORM_ANGLE_ANGLE,
+                                       reinterpret_cast<void*>(EGL_DEFAULT_DISPLAY),
+                                       displayAttribs);
+  if (egl_display_ == EGL_NO_DISPLAY) {
+    LOG_ERROR("Failed to get ANGLE EGL display");
     return;
   }
-  [EAGLContext setCurrentContext:egl_context_];
-  LOG_INFO("iOS OpenGL ES 2.0 context created successfully");
+
+  EGLint major, minor;
+  if (!eglInitialize(egl_display_, &major, &minor)) {
+    LOG_ERROR("Failed to initialize ANGLE EGL");
+    return;
+  }
+  LOG_DEBUG("ANGLE EGL initialized: version major:{} minor:{}", major, minor);
+
+  const EGLint configAttribs[] = {
+      EGL_RED_SIZE, 8,
+      EGL_GREEN_SIZE, 8,
+      EGL_BLUE_SIZE, 8,
+      EGL_ALPHA_SIZE, 8,
+      EGL_SURFACE_TYPE, EGL_PBUFFER_BIT,
+      EGL_RENDERABLE_TYPE, EGL_OPENGL_ES2_BIT,
+      EGL_NONE};
+
+  EGLint numConfigs;
+  if (!eglChooseConfig(egl_display_, configAttribs, &egl_config_, 1,
+                       &numConfigs) ||
+     numConfigs < 1) {
+    LOG_ERROR("Failed to choose ANGLE EGL config");
+    return;
+  }
+
+  const EGLint contextAttribs[] = {EGL_CONTEXT_CLIENT_VERSION, 2, EGL_NONE};
+  egl_context_ = eglCreateContext(egl_display_, egl_config_, EGL_NO_CONTEXT,
+                                  contextAttribs);
+  if (egl_context_ == EGL_NO_CONTEXT) {
+    LOG_ERROR("Failed to create ANGLE EGL context");
+    return;
+  }
+
+  // Offscreen only - real presentation goes through the Metal/IOSurface
+  // interop bridge (adapters/beauty/interop_apple.mm), not this surface.
+  const EGLint pbufferAttribs[] = {EGL_WIDTH, 1, EGL_HEIGHT, 1, EGL_NONE};
+  egl_surface_ =
+      eglCreatePbufferSurface(egl_display_, egl_config_, pbufferAttribs);
+  if (egl_surface_ == EGL_NO_SURFACE) {
+    LOG_ERROR("Failed to create ANGLE EGL surface");
+    return;
+  }
+
+  if (!eglMakeCurrent(egl_display_, egl_surface_, egl_surface_, egl_context_)) {
+    LOG_ERROR("Failed to make ANGLE EGL context current");
+    return;
+  }
+  LOG_INFO("iOS ANGLE EGL context created successfully");
 #elif defined(GPUPIXEL_MAC)
   LOG_DEBUG("Creating macOS OpenGL context");
   NSOpenGLPixelFormatAttribute pixelFormatAttributes[] = {
@@ -228,9 +282,9 @@
 
 void GPUPixelContext::UseAsCurrent() {
 #if defined(GPUPIXEL_IOS)
-  if ([EAGLContext currentContext] != egl_context_) {
-    LOG_TRACE("Setting current EAGLContext");
-    [EAGLContext setCurrentContext:egl_context_];
+  if (eglGetCurrentContext() != egl_context_) {
+    LOG_TRACE("Setting current ANGLE EGL context");
+    eglMakeCurrent(egl_display_, egl_surface_, egl_surface_, egl_context_);
   }
 #elif defined(GPUPIXEL_MAC)
   if ([NSOpenGLContext currentContext] != image_processing_context_) {
@@ -255,7 +309,8 @@
 
 void GPUPixelContext::PresentBufferForDisplay() {
 #if defined(GPUPIXEL_IOS)
-  [egl_context_ presentRenderbuffer:GL_RENDERBUFFER];
+  // Offscreen pbuffer, same as GPUPIXEL_ANDROID below - the result reaches
+  // the screen through the Metal/IOSurface interop bridge, not this surface.
 #elif defined(GPUPIXEL_MAC)
   // No implementation needed
 #elif defined(GPUPIXEL_ANDROID)
@@ -267,7 +322,7 @@
 
 void GPUPixelContext::ReleaseContext() {
   LOG_DEBUG("Releasing OpenGL context");
-#if defined(GPUPIXEL_ANDROID)
+#if defined(GPUPIXEL_ANDROID) || defined(GPUPIXEL_IOS)
   if (egl_display_ != EGL_NO_DISPLAY) {
     eglMakeCurrent(egl_display_, EGL_NO_SURFACE, EGL_NO_SURFACE,
                    EGL_NO_CONTEXT);
